# DrfitSentinel
DriftSentinel monitors RAG pipelines for silent retrieval failures. It detects content drift (stale embeddings), embedding drift (model changes), and query drift (coverage gaps) using similarity and hash-based signals, then auto-remediates or escalates fixes to a human via a Streamlit dashboard.
